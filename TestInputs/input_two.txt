program input_two;

class First {
    attributes
        private float amount;

    methods
        public func float getTotalAmount() {
            return(amount * ((5.0 + 34) / 12) - -5);
        }
};

vars 
    int a;
    float b;

main() {
    vars
        First wallet;
        int c;

    if (wallet.getTotalAmount() > 100 and not wallet.getTotalAmount() > 1000 or 3.1416 == 0) then {
        print(-a + -b - +c);
    }
}